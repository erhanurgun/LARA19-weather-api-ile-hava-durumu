{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "watch": "vite -- --watch",
        "serve": "php artisan serve --host=laravel.test --port=80"
    },
    "devDependencies": {
        "axios": "^1.6.2",
        "laravel-vite-plugin": "^0.8.1",
        "vite": "^4.5.0"
    }
}
